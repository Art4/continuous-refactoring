# tooling-tree

Tickets about the **generic** tooling tree — `skills/refactor-scan/references/tooling-tree.md`, the
language-neutral root (`git` → `loop-config` → `structural-scan`) every language specialization attaches
to. Kept separate from `.scratch/php-tooling-tree/` (the PHP specialization's own tickets), since these
concern the shape shared across every future language tree, not PHP-specific nodes or fulfilment checks.

## Issues

| # | Ticket | Wave |
|---|--------|------|
| 01 | `.editorconfig` as its own generic-tree node, before `php-cs-fixer` | first |
