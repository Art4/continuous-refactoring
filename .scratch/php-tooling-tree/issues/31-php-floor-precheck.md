# 31 — Check the PHP floor once before walking the deterministic PHP tooling leaves individually

**What to build:** Before proposing `php-cs-fixer`, `phpunit`, `composer-audit`, `phpstan-level-0-baseline`, or `test-runner-if-missing` one at a time, check the target's current PHP floor (`composer.json`'s `require.php` / `config.platform.php`) once against each leaf's known minimum version. A leaf whose minimum isn't met yet is skipped (or batch-recorded as out-of-scope with a `**Blocked by:** PHP >= X.Y` entry, ticket 10's mechanical-reversal shape) in one pass instead of five separate propose → design → implement → reject cycles.

**Why:** Observed on a real dry run (`Art4/legacy-todo`, PHP 5.6 floor): all five deterministic PHP tooling leaves got individually proposed, individually filed as candidates, and individually rejected as `wontfix` across five separate passes — each rejection citing the same underlying PHP-version wall (`docs/refactoring/out-of-scope/{php-cs-fixer,phpunit,composer-audit,phpstan-level-0-baseline,test-runner-if-missing}.md`). Five round trips to discover one fact five times.

**Blocked by:** 06 ✓ done — first-wave node walk (ADR-0005); reuses ticket 10's `**Blocked by:** PHP >= X.Y` out-of-scope field and `detect_nodes()`'s version-comparison helpers (`refactor/composer-audit-eligibility` branch) as the mechanical building blocks — this ticket is their forward counterpart (skip/batch-reject up front) to that branch's reversal detection (un-reject once the floor rises).

**Status:** done

- [x] `tooling_tree.py` (or the manual tree-walk fallback) checks the target's current PHP floor once per pass, against a per-leaf minimum-version table for the five deterministic PHP tooling leaves — `_LEAF_MIN_PHP_VERSION` + `php_floor_precheck()` in `skills/refactor-scan/references/tooling_tree.py`
- [x] A leaf below the floor is skipped from `next`/proposals without a separate propose/design/implement/reject cycle per leaf — wired into `next_candidates()` and `roadmap()`
- [x] Recorded out-of-scope entries (if any get filed at all under this design) carry `**Blocked by:** PHP >= X.Y` from the start, consistent with ticket 10's format — N/A under the chosen design, see next line
- [x] Design decision: skip silently (no out-of-scope entry until something actually asks) vs. batch-file all five `out-of-scope/` entries in one pass — **skip silently**, chosen: the check is cheap and fully re-derived from `composer.json` every pass, so nothing is lost by not persisting it, and `out-of-scope/` otherwise records a genuine human/agent rejection decision, not a mechanical fact already on disk. Reasoning and its one real consequence (four of the five leaves are themselves `structural-scan` leaves, which stays closed while they're floor-blocked) written up in `php-tooling-tree.md`'s new "PHP floor precheck" section and in `php_floor_precheck()`'s docstring.

## Comments

> **2026-08-29:** Filed while reviewing `.scratch/legacy-todo-loop-observation/findings.md` before deleting it (its Runde 8 finding) — not implemented in `refactor/composer-audit-eligibility`, which only built the reversal direction (ticket 10). Recorded here so the idea survives the findings-log cleanup without being built prematurely.

> **2026-08-30:** Implemented. Per-leaf minimum PHP version table is each tool's true hard floor — the oldest PHP release any published major line of that tool has ever run on, not just its current/latest major's requirement — since the suite's own observed practice (adopting the oldest still-PHP-compatible line, e.g. PHPUnit ^5.7 for a PHP 5.6 target) means a tool stays adoptable as long as *some* published version of it can install, however old. Concretely: `php-cs-fixer`/`phpunit`/`test-runner-if-missing` floor at PHP 5.3 (their earliest Composer-installable lines); `composer-audit` floors at PHP 7.2 (the `composer audit` subcommand only ever shipped in Composer >=2.4, which itself requires PHP >=7.2.5); `phpstan-level-0-baseline` floors at PHP 7.0 (phpstan/phpstan's first published release, 0.1, required PHP ~7.0; its Psalm equivalent has likewise required PHP 7+ since its earliest releases). Under this table a PHP 5.6 target — as in the observed dry run — blocks only `composer-audit` and `phpstan-level-0-baseline`, not all five; the other three genuinely have PHP-5.6-compatible lines. 9 new tests in `scripts/test_tooling_tree.py`'s `PhpFloorPrecheckTests`; full suite (141 tests via `unittest discover`) and all six `fixtures/harness/run.sh roadmap` fixtures stay green. Ran `/code-review since main` (Standards + Spec sub-agents) before this write-up: fixed an em-dash/ASCII-dash idiom mismatch flagged on Standards, wired the same skip into `withheld_candidates()` for topology-change robustness and corrected the phpstan floor from a wrongly-cited 7.1 to the verified-from-source 7.0, both flagged on Spec (which fetched phpstan's 0.1 tag `composer.json` directly to check the claim).
