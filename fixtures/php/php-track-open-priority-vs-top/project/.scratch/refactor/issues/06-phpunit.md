# PHPUnit

**Status:** open
**Labels:** refactor:candidate
**Filed:** 2026-01-01

## Where

Suite-wide (tooling adoption, no single file).

## Problem

`phpunit` is unfulfilled — no dev dependency, no config, no test suite has run yet.

## Plan

Add `phpunit/phpunit` as a dev dependency, commit a `phpunit.xml` config, write one smoke test. MR scope: dependency + config + one test (per `skills/refactor-scan/references/php-tooling-tree/phpunit.md`).

## Comments

(none)
