{
    "name": "example/php-guardrails-scan-fills-open",
    "description": "Safety Net closed; Guardrails due with an empty Open and eleven unresolved scope nodes (some blocked) — the scan must record all of them",
    "require": {
        "php": "^8.1",
        "monolog/monolog": "^3.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.2",
        "friendsofphp/php-cs-fixer": "^3.0",
        "phpstan/phpstan": "^1.12",
        "rector/rector": "^1.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    }
}
