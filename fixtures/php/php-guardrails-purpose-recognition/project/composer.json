{
    "name": "example/php-guardrails-purpose-recognition",
    "description": "Safety Net closed; composer-audit gated in CI only indirectly, via a composer.json script, no literal 'composer audit' text in any CI workflow file",
    "require": {
        "php": "^8.1",
        "monolog/monolog": "^3.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.2",
        "friendsofphp/php-cs-fixer": "^3.0",
        "phpstan/phpstan": "^1.12",
        "rector/rector": "^1.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "security-check": "composer audit"
    }
}
