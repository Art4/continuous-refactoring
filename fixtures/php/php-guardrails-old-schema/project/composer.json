{
    "name": "example/php-guardrails-old-schema",
    "description": "Mid-migration target: ## Safety Net already closed (its own Track has already run once), Fulfilled nodes/Pending candidates still carry old-schema residue, no ## Guardrails section yet",
    "require": {
        "php": "^8.1",
        "monolog/monolog": "^3.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.2",
        "friendsofphp/php-cs-fixer": "^3.0",
        "phpstan/phpstan": "^1.12",
        "rector/rector": "^1.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    }
}
