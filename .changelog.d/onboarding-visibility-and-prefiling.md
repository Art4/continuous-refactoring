Every currently-unblocked tooling-tree node now gets a minimal candidate issue filed as soon as it's proposable, not just the one the loop picks to work on next — the same way `structural-scan` already surfaces every finding it discovers, giving a human a chance to comment or reject a proposed tool before any implementation work is spent on it. `loop-config`'s own onboarding interview now summarizes the concrete decisions and actions about to happen before writing anything, and prints a short status line for each one as it runs; a cold-start pass on a target with no Refactoring Notes yet also skips straight to proposing `loop-config` instead of walking the whole tooling tree first.
