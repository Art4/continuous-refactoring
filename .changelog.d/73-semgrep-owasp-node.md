New `semgrep` tooling-tree node (OWASP Top 10 static analysis, Signal-producing — no `structural-scan` gating): complements the existing `psalm-taint-analysis` node rather than duplicating it (crypto misuse, misconfiguration, and logging-gap categories taint analysis doesn't reach on its own). Recommended parent `psalm-taint-analysis`, so Psalm's own taint-flow baseline is decided first. No separate manual security checklist — the tool's own CI-gated findings supersede it.
