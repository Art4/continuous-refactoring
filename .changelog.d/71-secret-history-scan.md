`refactor-scan` now runs a one-time full git-history secret scan once `secret-detection` is fulfilled, reusing the same scanner already gating CI and its own baseline mechanism to avoid re-filing known findings — each new finding becomes a `refactor:priority` candidate issue (the secret's value redacted), filed by `refactor-learn`. Closes the retroactive half of `secret-detection`'s own scope (ticket 13), left open on purpose when the CI-gate node itself shipped.
