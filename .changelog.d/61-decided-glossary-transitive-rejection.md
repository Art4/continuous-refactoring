Clarified `CONTEXT.md`'s `Recommended edge`/`decided` definition and `php-tooling-tree.md`'s `Nodes` preamble: a recommended parent counts as decided-rejected not only via its own `out-of-scope/` entry, but also transitively, when one of its own required ancestors is rejected — a rule the tooling has always implemented (ADR-0016) but the glossary never stated, which could make a correctly-unblocked candidate look like a script regression when checked against the incomplete definition.
