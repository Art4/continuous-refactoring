`refactor-prioritize` now selects and minimally files a structural or PHPStan-baseline-shrink candidate itself (a second, fresh dispatch — "Select mode"), the moment one is chosen — visible on the tracker right away instead of only once its full plan is finished. `refactor-design` grounds and grills that already-filed candidate afterward, adding the plan as a comment rather than filing a second issue.
